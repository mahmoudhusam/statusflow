{
  "name": "@statusflow/mcp-server",
  "version": "0.0.1",
  "description": "MCP Server for StatusFlow",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "groq-sdk": "0.37.0",
    "dotenv": "^17.2.0"
  },
  "devDependencies": {
    "typescript": "5.8.2",
    "ts-node-dev": "^2.0.0"
  }
}
